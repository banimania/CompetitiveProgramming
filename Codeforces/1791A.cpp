#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void solve() {
  char c;
  cin >> c;

  cout << ((c == 'c' || c == 'o' || c == 'd' || c == 'e' || c == 'f' || c == 'o' || c == 'r' || c == 'c' || c == 'e' || c == 's') ? "YES" : "NO") << endl;
}

int main() {
  int t;
  cin >> t;
  while (t--) solve();
  return 0;
}
