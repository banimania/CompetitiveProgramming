#include <bits/stdc++.h>
using namespace std;

void dfs(int curr, vector<int> &tp, map<int, int> &tin, vector<int> &ans) {

}

int main() {
  int n;
  cin >> n;

  map<int, int> tin;
  vector<int> tp(n);
  for (int i = 0; i < n; i++) {
    cin >> tp[i];
    tp[i]--;
  }
  return 0;
}
